Programmed in Java with JDK8

Main() defined under Driver.java.

The program takes in up to two arguments: String1 and String2
To enable verbose mode, set String1 to "-verbose" and String2 to the directory to the input file.
To use normal mode, set String1 to the directory to the input file.